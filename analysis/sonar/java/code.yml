jobs:
  - job: SonarQubeAnalysis_node
    displayName: 🛡️Sonarqube Analysis🛡️🚦
    dependsOn: initJob
    condition: not(eq(variables[dependencies.initJob.outputs['init.repoName']],'true'))
    steps:
      - template: main.yml
        parameters:
          workPath: ${{ parameters.workPath }}
          